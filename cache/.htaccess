# The cache holds generated PHP that the forum includes. It is never served.
# Apache 2.4 authorisation, with the 2.2 form for hosts that still run it.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
