<?php /* The addresses a user posted from, each with when it was used last and how often. Every variable is escaped unless it is Html. */ ?>
	<div class="main-head">
<?php if ($headOptions !== null): ?>

		<p class="options"><?= $headOptions ?></p><?php endif ?>
		<h2 class="hn"><span><?= $heading ?></span></h2>
	</div>
	<div class="main-content main-forum">
		<table>
			<thead>
				<tr>
					<?= $header, "\n" ?>
				</tr>
			</thead>
			<tbody>
<?php foreach ($rows as $row): ?>
<?= $row['before'] ?>
				<tr class="<?= $row['style'] ?>">
					<?= $row['cells'], "\n" ?>
				</tr>
<?php endforeach ?>
			</tbody>
		</table>
	</div>
	<div class="main-foot">
<?php if ($footOptions !== null): ?>

		<p class="options"><?= $footOptions ?></p><?php endif ?>
		<h2 class="hn"><span><?= $heading ?></span></h2>
	</div>
